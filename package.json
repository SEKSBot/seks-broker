{
  "name": "seks-broker",
  "version": "0.1.0",
  "description": "Cloud-native secret management for AI agents",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute seks-broker-db --local --file=./schema.sql",
    "db:migrate:prod": "wrangler d1 execute seks-broker-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.28.0"
  }
}
