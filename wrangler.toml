name = "seks-broker"
main = "src/index.ts"
compatibility_date = "2024-02-01"

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "seks-broker-db"
database_id = "placeholder-will-be-replaced-on-first-deploy"

# Environment variables (set via wrangler secret)
# MASTER_KEY - 32-byte hex-encoded encryption key
